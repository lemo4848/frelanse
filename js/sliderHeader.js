if(window.matchMedia("(max-width:820px)").matches){const slider=document.querySelector('#slider');const btnNext=document.querySelector('.header__slider-button');const btnPrev=document.querySelector('.header__slider-button2');const sliderItems=Array.from(slider.children);sliderItems.forEach(function(slide,index){if(index!==0){slide.classList.add('hidden')}
slide.dataset.index=index;sliderItems[0].setAttribute('data-active','')})
btnNext.onclick=function(){const currensSlide=slider.querySelector('[data-active]');const currenSlideIndex=+currensSlide.dataset.index;currensSlide.classList.add('hidden');currensSlide.removeAttribute('data-active');let nextSlideIndex=currenSlideIndex+1===sliderItems.length?0:currenSlideIndex+1;const nextSlide=slider.querySelector(`[data-index="${nextSlideIndex}"]`)
nextSlide.classList.remove("hidden");nextSlide.setAttribute('data-active',"")};btnPrev.onclick=function(){const currensSlide=slider.querySelector('[data-active]');const currenSlideIndex=+currensSlide.dataset.index;currensSlide.classList.add('hidden');currensSlide.removeAttribute('data-active');let nextSlideIndex=currenSlideIndex===0?sliderItems.length-1:currenSlideIndex-1;const nextSlide=slider.querySelector(`[data-index="${nextSlideIndex}"]`)
nextSlide.classList.remove("hidden");nextSlide.setAttribute('data-active',"")}}